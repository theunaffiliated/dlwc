# David L.W. Casimes — personal site

Static site, no build step. `index.html` (home), `experience.html`, `styles.css`, `favicon.svg`.

Publish: upload these files to the repository root and deploy from `main` / `(root)` in Settings → Pages.
